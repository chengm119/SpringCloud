spring:
  cloud:
    config:
      name: spring-cloud
      profile: dev
      #label: master
      discovery:
        enabled: true
        serviceId: spring-cloud-config-server
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8000/eureka/
management:
  endpoints:
    web:
      exposure:
        include: refresh